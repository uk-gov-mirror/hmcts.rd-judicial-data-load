spring:
  cloud:
    propertiesvolume:
      prefixed: false
      paths: /mnt/secrets/rd
      aliases:
        judicial-api-POSTGRES-PASS: spring.datasource.password
        ApplicationInsightsInstrumentationKey: azure.application-insights.instrumentation-key
        ACCOUNT-NAME: azure.storage.account-name
        ACCOUNT-KEY: azure.storage.account-key
        CONTAINER-NAME: azure.storage.container-name
        judicial-topic-primary-connection-string: JRD_ASB_CONNECTION_KEY_STR
        LD-SDK-KEY: LD_SDK_KEY
        OAUTH2-CLIENT-SECRET: idam.api-redirect-uri
        OAUTH2-AUTH: idam.api.authorization
        OAUTH2-CLIENT-AUTH: idam.api.client-authorization
        rd-sendgrid-api-key: sendgrid.key

